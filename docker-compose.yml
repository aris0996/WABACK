services:
  wa-ai-memory-bot:
    build: .
    container_name: wa-ai-memory-bot
    env_file:
      - .env
    network_mode: host
    volumes:
      - ./:/app
      - ./data:/app/data
    restart: unless-stopped
