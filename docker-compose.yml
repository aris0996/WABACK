services:
  wa-ai-memory-bot:
    build: .
    container_name: wa-ai-memory-bot
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
